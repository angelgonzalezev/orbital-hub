{
  "name": "orbital-docs",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "vitepress dev --port 3001",
    "build": "rm -rf ../public/docs && vitepress build",
    "serve": "vitepress preview --port 3001"
  },
  "devDependencies": {
    "vitepress": "^1.6.4"
  }
}
